apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  labels:
    app: inventory-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: inventory-service
  template:
    metadata:
      labels:
        app: inventory-service
    spec:
      imagePullSecrets:
      - name: ecr-registry-secret
      containers:
      - name: inventory-service
        image: 905418111634.dkr.ecr.eu-west-1.amazonaws.com/springboot-kafka-microservices/inventory-service:latest
        ports:
        - containerPort: 8082
          name: http
        env:
        - name: DB_URL
          valueFrom:
            configMapKeyRef:
              name: inventory-service-config
              key: DB_URL
        - name: DB_USER
          valueFrom:
            configMapKeyRef:
              name: inventory-service-config
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: inventory-service-secret
              key: DB_PASSWORD
        - name: KAFKA_BOOTSTRAP_SERVERS
          valueFrom:
            configMapKeyRef:
              name: inventory-service-config
              key: KAFKA_BOOTSTRAP_SERVERS
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /api/inventory
            port: 8082
          initialDelaySeconds: 60
          periodSeconds: 10
          failureThreshold: 5
        readinessProbe:
          httpGet:
            path: /api/inventory
            port: 8082
          initialDelaySeconds: 30
          periodSeconds: 5
          failureThreshold: 3


